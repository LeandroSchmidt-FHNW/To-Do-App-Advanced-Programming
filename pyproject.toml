[project]
name = "todo-app"
version = "0.1.0"
description = "NiceGUI to-do app using OOP, ORM, and a layered MVC-inspired structure."
requires-python = ">=3.10"

dependencies = [
    "nicegui",
    "sqlmodel",
]

[tool.pytest.ini_options]
pythonpath = ["."]
